# Enable ingress in minikube
minikube addons enable ingress

# Apply ingress resource
kubectl apply -f messaging_app/ingress.yaml

# Verify ingress is running
kubectl get ingress

# (Optional) Add to /etc/hosts for local testing:
# echo "$(minikube ip) django.local" | sudo tee -a /etc/hosts

# Test the ingress routing
curl http://django.local/api/
